<!DOCTYPE html>
<html>
  <head>
    <title>Суетной Сайт</title>
    <meta name="viewport" content="width=device-width,initial-scale=1">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
    
    <%= javascript_include_tag "https://code.jquery.com/jquery-3.6.1.js" %>
    <%= javascript_include_tag "https://cdn.jsdelivr.net/npm/bootstrap@5.2.1/dist/js/bootstrap.bundle.min.js" %>
    <%= stylesheet_link_tag "https://cdn.jsdelivr.net/npm/bootstrap@5.2.1/dist/css/bootstrap.min.css" %>


  </head>

  <body>
    <header>
      <nav class="navbar navbar-expand-lg bg-dark navbar-dark navbar-nav">
        <div class="container-fluid">
          <a class="navbar-brand" href="#">SUETA</a>
          <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarSupportedContent" aria-controls="navbarSupportedContent" aria-expanded="false" aria-label="Toggle navigation">
            <span class="navbar-toggler-icon"></span>
          </button>
          <div class="collapse navbar-collapse" id="navbarSupportedContent">
            <ul class="navbar-nav me-auto mb-2 mb-lg-0">
              <li class="nav-item">
                <a class="nav-link" aria-current="page" href="/">Домашняя</a>
              </li>
              <li class="nav-item">
                <a class="nav-link" href="/main/ipl">Языки интернет-программирования</a>
              </li>
              <li class="nav-item">
                <a class="nav-link" href="/main/contacts">Контакты</a>
              </li>
            </ul>
            <ul class="nav justify-content-end">
              <% if current_user.present? %>
                <li class="nav-item">
                  <%= link_to "#{current_user.nickname}", "/users/#{current_user.id}", class: "btn btn-link btn-sm nav-link link-secondary" %>
                </li>
                <li class="nav-item">
                  <%= button_to 'Выйти', session_path, method: :delete, class: "btn btn-link btn-sm nav-link link-secondary" %>
                </li>
              <% else %>
                <li class="nav-item">
                  <a class="nav-link link-secondary" href="/session/new">Авторизация</a>
                </li>
              <% end %>
            </ul>
          </div>
        </div>
      </nav>
	  </header>

    <main class="container">
      <% if flash[:notice].present?%>
        <div class="alert alert-success" role="alert">
          <%= flash[:notice] %>
        </div>
      <% elsif flash[:alert].present?%>
        <div class="alert alert-danger" role="alert">
          <%= flash[:alert] %>
        </div>
      <% end %>

      <% if current_user.present? %>
        <% if BannedUser.find_by(user_id: current_user.id).present? %>
          <% session.delete(:user_id) %>
          <div class="alert alert-danger" role="alert">
            <span class="fw-bold">Вы были заблокированы!</span>
          </div>
        <% end %>
      <% end %>
      
      <%= yield %>
    </main>

    <footer class="bg-dark text-center text-white">
      <!-- Copyright -->
      <div class="text-center p-3" style="background-color: rgba(0, 0, 0, 0.2);">
        © 2022 Все права защищены:
        <a class="text-white" href="https://vk.com/id240730961">sueta1995</a>
      </div>
      <!-- Copyright -->
	  </footer>
  </body>
</html>
